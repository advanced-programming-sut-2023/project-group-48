package model.Buildings;

import model.User;

public class PitchDitch extends Building{
    public PitchDitch(User owner, int column, int row, String type) {
        super(owner, column, row, type);
    }
}